{
  "name": "@mono/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "check-types": "convex codegen --typecheck disable && tsc -p convex/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "better-auth": "catalog:",
    "convex": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@mono/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "^5"
  }
}
